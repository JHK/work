# `docs/explanation/`

An explanation doc carries reasoning: why a structure, a partition or a tool choice is shaped the way it is, which alternatives lost and on what grounds, and what the model deliberately does not solve. It describes the model, never a moment in time.

Open one when a change would work against a design decision, or when the same *why* keeps being re-derived.
