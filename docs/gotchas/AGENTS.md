# `docs/gotchas/`

A gotcha is a warning written for agents, in three lines: symptom, cause, what to do instead. The bar is a recurring trap where the note would have saved a session a multi-step detour.

Entries are deleted once the underlying cause is fixed in code or config.

Open the one covering your area before you start.
