# `docs/rules/`

A rules doc states the invariants of one domain as observable facts — "X cannot reach Y" — each with a stable identifier and a named enforcement mechanism. Identifiers are append-only and citable from other docs.

Skim the filenames and title lines first. Open the one covering a domain before changing anything inside it.
